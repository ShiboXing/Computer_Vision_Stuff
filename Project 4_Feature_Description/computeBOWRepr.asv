function [bow_repr] = computeBOWRepr(features, means)
   %II1
   bow=zeros(1,size(means,2));
   rlen=size(features,1);
   %II2
   distances=pdist2(features,means);
   
   %II3
   [V,I]=min(distances,[],2);
   for i=1:size(I,1)
      bow(I(i,1))= 
   end
   
   
end